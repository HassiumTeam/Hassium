func main () {
	foo ();	
}

func foo () {
	a = 3;
	try {
		bar();
		println ("You can't see me");
	} catch {
		println ("Caught: " + value.tostring() + a);
		println ("My time is now");
	}
}

func bar () {
	baz();
}

func baz () {
	x = 2;
	raise x;
}

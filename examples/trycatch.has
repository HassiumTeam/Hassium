func main () {
	foo ();	
}

func foo () {
	a = 3;
	try {
		bar();
		println ("You can't see me");
	} catch (except) {
		println ("Caught: " + except.tostring () + a);
		println ("My time is now");
	}
}

func bar () {
	baz();
}

func baz () {
	x = 2;
	raise x;
}

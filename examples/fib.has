func main () {
    print (fib (10));
}

# Recursive fib function
func fib (n) {
    if (n == 0) {
        return 0;
    } else if (n == 1) {
        return 1;
    }
    return fib (n - 1) + fib (n - 2);
}

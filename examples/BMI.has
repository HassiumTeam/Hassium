func calculateBMI(height, weight) {
	return weight / (height * height);
}

func main() {
	print("How tall are you? ");
	height := Convert.toNumber(input());
	print("How much do you weigh? ");
	weight := Convert.toNumber(input());

	println(calculateBMI(height, weight));
}

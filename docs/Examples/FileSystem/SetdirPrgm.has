$SUMMARY: Sets and displays the current directory$

print("Current directory: ", getdir(), "\nEnter a new directory: ");
setdir(input());

print("Now the current directory is: ", getdir(), "\n");

exit(0);

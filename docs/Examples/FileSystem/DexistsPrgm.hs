$SUMMMARY: Determins if the directory entered by the user exists$

print("Enter a directory path to see if it exists: ");
path := input();

print("Does directory exist? ", dexists(path), "\n");

exit(0);

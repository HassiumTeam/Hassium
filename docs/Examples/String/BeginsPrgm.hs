$SUMMARY: Gets a boolean indicating if a string starts with another string$

print("Enter a string: ");
str := input();

if (begins(str, "Jaco")) {
	print("The string starts with Jaco!");
} else {
	print("The string doesn't start with Jaco :(");
}

print("\n");

exit(0);

$SUMMARY: Concatanates two strings into a result string and displays it$

print("Enter the first string: ");
first := input();

print("Enter the second string: ");
second := input();

print("The result string is: ", strcat(first, second), "\n");

exit(0);

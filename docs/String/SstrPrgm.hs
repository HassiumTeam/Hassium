$SUMMARY: Gets a substring from a specified string$

print("Enter a string: ");
str := input();

print("Enter the startindex for the substring: ");
start := input();

print("Enter the length of the substring: ");
len := input();

print("The substring is: ", sstr(str, start, len), "\n");

exit(0);

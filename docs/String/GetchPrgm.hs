$SUMMARY: Gets the specified character from the specified string and returns it$

print("Enter a string: ");
str := input();

print("Enter the character index to get: ");
num := input();

print("The ", num, " character in the string ", str, " is: ", getch(str, num), "\n");

exit(0);
